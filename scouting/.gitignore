# Runtime state and generated output — not source, regenerated by running
# the pipeline. Keeping these out of git keeps the history to reviewable code
# and avoids growing the repo indefinitely with dated personal job-search data.
state/
reports/
